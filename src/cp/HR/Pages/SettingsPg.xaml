<Page x:Class="HR.Pages.SettingsPg"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:HR.Pages"
      xmlns:uc="clr-namespace:HR.Controls"
      xmlns:utils="clr-namespace:HR.Utilities"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPg">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <utils:InverseBoolToVisibilityConverter x:Key="InvBoolConv"/>
    </Page.Resources>
    <ScrollViewer Style="{StaticResource MainView}">
        <StackPanel>
            <uc:Header HeaderText="Безопасность"></uc:Header>
            <TextBlock Style="{StaticResource Paragraph}">
                Данный раздел содержит информацию о пользовательских настройках, определяющих способ доступа к учетной записи.
                Здесь можно поменять текущий пароль.
                Для обеспечения большей безопасности рекомендуется включить двухэтапную аутентификацию.
            </TextBlock>
            <Grid x:Name="MainGrid" Margin="0 30 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="FirstSection" MinWidth="300" Margin="5" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Label Foreground="{StaticResource magentaDarkBrush}" FontSize="16">Смена пароля</Label>
                    <uc:Details Type="Question"
                                ToolTip="Для смены пароля необходимо введение текущего используемого пароля и нового пароля с подтверждением (повторным введением). Устанавливаемый пароль должен соответствовать основным требованиям безопасности. (содержать не менее 8 символов, включая буквы в разных регистрах, цифры и специальные символы)."
                                />
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="300"/>
                        <ColumnDefinition Width="2*" MaxWidth="400" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label Style="{StaticResource FormLabel}" ToolTip="Текущий пароль">Текущий пароль</Label>
                    <DockPanel Grid.Column="1" Margin="0 10">
                        <uc:IconHighlightButton Width="20" Height="20" Margin="10 5 30 5" ToolTip="Показать пароль" DockPanel.Dock="Right"
                                >
                            <StackPanel>
                                <Viewbox Stretch="Fill" Width="20" Height="16">
                                    <Path Fill="{StaticResource BlueBrush}" Data="{StaticResource EyeSolidPath}"/>
                                </Viewbox>
                            </StackPanel>
                        </uc:IconHighlightButton>
                        <PasswordBox x:Name="CurPwdPwb" />
                    </DockPanel>
                    <Label Grid.Row="1" Style="{StaticResource FormLabel}" ToolTip="Новый пароль">Новый пароль</Label>
                    <DockPanel Grid.Column="1" Grid.Row="1" Margin="0 10">
                        <uc:IconHighlightButton Width="20" Height="20" Margin="10 5 30 5" ToolTip="Показать пароль" DockPanel.Dock="Right"
                                                >
                            <StackPanel>
                                <Viewbox Stretch="Fill" Width="20" Height="16">
                                    <Path Fill="{StaticResource BlueBrush}" Data="{StaticResource EyeSolidPath}"/>
                                </Viewbox>
                            </StackPanel>
                        </uc:IconHighlightButton>
                        <PasswordBox x:Name="NewPwdPwb" />
                    </DockPanel>
                    <Label Grid.Row="2" Style="{StaticResource FormLabel}" ToolTip="Повтор пароля">Повтор пароля</Label>
                    <DockPanel Grid.Column="1" Grid.Row="2" Margin="0 10">
                        <uc:IconHighlightButton Width="20" Height="20" Margin="10 5 30 5" ToolTip="Показать пароль" DockPanel.Dock="Right"
    >
                            <StackPanel>
                                <Viewbox Stretch="Fill" Width="20" Height="16">
                                    <Path Fill="{StaticResource BlueBrush}" Data="{StaticResource EyeSolidPath}"/>
                                </Viewbox>
                            </StackPanel>
                        </uc:IconHighlightButton>
                        <PasswordBox x:Name="RepeatPwdPwb" />
                    </DockPanel>
                </Grid>
                    <Grid Margin="0 10 0 0" HorizontalAlignment="Right">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="ResetBtn" Grid.Column="0" Content="Отменить" IsCancel="True"
                                Style="{StaticResource BlueBtn}"
                                IsEnabled="{Binding IsEnabled}"
                                Click="ResetBtn_Click"
                                />
                        <Button x:Name="SignInBtn" Grid.Column="2" IsDefault="True"
                                Style="{StaticResource GreenBtn}"
                                IsEnabled="{Binding IsEnabled}"
                                Click="SubmitBtn_Click"
                                >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Сохранить" Visibility="{Binding IsInProgress, Converter={StaticResource InvBoolConv}}" />
                                <uc:Dots Visibility="{Binding IsInProgress, Converter={StaticResource BoolToVis}}" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
                <StackPanel x:Name="SecondSection" MinWidth="300" Margin="5" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Label Foreground="{StaticResource magentaDarkBrush}" FontSize="16">Безопасный вход</Label>
                    <uc:Details Type="Question"
                                ToolTip="Для обеспечения безопасного входа необходимо настроить и подключить двухэтапную аутентификацию"
                                />
                    </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <CheckBox Content="Включить двухэтапную аутентификацию"
                          IsEnabled="{Binding IsInProgress, Converter={StaticResource InvBoolConv}}"
                          IsChecked="{Binding Is2FA}"
                          />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>

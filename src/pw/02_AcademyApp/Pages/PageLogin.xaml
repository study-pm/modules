<Page x:Class="AcademyApp.Pages.PageLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AcademyApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageLogin">
    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0 5"/>
        </Style>
        <Style TargetType="Button" x:Key="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        <ControlTemplate TargetType="Button" x:Key="btnTmpl">
            <Border CornerRadius="5" x:Name="border"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="2"
                    Background="{TemplateBinding Background}"
                    Height="{TemplateBinding Height}"
                    Width="{TemplateBinding Width}" >
                <ContentControl Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Focusable="False"
                                Content="{TemplateBinding Content}" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#A0D6A0"/>
                    <!-- Change to desired hover background -->
                    <Setter TargetName="border" Property="BorderBrush" Value="#7FBF7F"/>
                    <!-- Change to desired hover border color -->
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <!-- Change to desired hover text color -->
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Page.Resources>

    <Grid>
        <StackPanel MaxWidth="250">
            <TextBlock Style="{StaticResource Label}">Введите логин:</TextBlock>
            <TextBox Padding="5" Margin="0 0 0 5" FontSize="14" x:Name="txbLogin"></TextBox>
            <TextBlock Style="{StaticResource Label}">Введите пароль:</TextBlock>
            <PasswordBox Padding="5" Margin="0 5" FontSize="14" Name="psbPassword"></PasswordBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Войти" Style="{StaticResource Button}" Template="{StaticResource btnTmpl}" BorderBrush="#549d5e" Background="#87e08e" Click="Button_Click" />
                <Button Style="{StaticResource Button}" BorderBrush="#cfa95b" Background="#ffd000" MouseEnter="Button_MouseEnter" MouseLeave="Button_MouseLeave" >Регистрация</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
